# Local SQLite (libSQL) database file. Override to point elsewhere (e.g. on a homelab).
DB_URL=file:./data/ti4.db

# Port the Node server listens on (serves /api, and the built web app in production).
PORT=3000
